/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['laraback.enactweb.com', "procash.enactweb.com", "assets.myntassets.com","fakestoreapi.com"],
  },
}

module.exports = nextConfig
